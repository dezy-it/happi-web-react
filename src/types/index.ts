export type IEventTypes = "TOKEN" | "MESSAGE" | "WELCOME" | "CONVERSATION_ID";

export enum EventTypes {
    TOKEN = "TOKEN",
    MESSAGE = "MESSAGE",
    WELCOME = "WELCOME",
    CONVERSATION_ID = "CONVERSATION_ID"
}